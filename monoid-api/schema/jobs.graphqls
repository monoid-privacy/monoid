scalar Time

enum JobStatus {
    QUEUED
    RUNNING
    COMPLETED
    FAILED
}

type Job {
    id: ID!
    jobType: String!
    resourceId: ID!
    status: JobStatus!
    createdAt: Time!
    updatedAt: Time!
}

type JobsResult {
    jobs: [Job!]
    numJobs: Int!
}

extend type Query {
    jobs(
        resourceId: ID!,
        jobType: String!,
        status: [JobStatus],
        limit: Int!,
        offset: Int!
    ): JobsResult!
}
